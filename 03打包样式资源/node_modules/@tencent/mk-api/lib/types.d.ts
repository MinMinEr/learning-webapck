import { AxiosInstance } from 'axios';
export interface PlainObj {
    [k: string]: any;
}
export declare type AxiosLike = AxiosInstance | Pick<AxiosInstance, 'request' | 'get' | 'patch' | 'post' | 'put' | 'delete'>;
export declare type StrOrNum = string | number;
/**
 * 分页类型
 *
 * @export
 * @interface PaginationParams
 */
export interface PaginationParams {
    offset?: number;
    limit?: number;
}
/**
 * 分页并且带搜索类型
 *
 * @export
 * @interface SearchWithPaginationParams
 * @extends {PaginationParams}
 */
export interface SearchWithPaginationParams extends PaginationParams {
    search?: string;
}
/**
 * 分页并且带排序类型
 *
 * @export
 * @interface SortWithPaginationParams
 * @extends {PaginationParams}
 */
export interface SortWithPaginationParams extends PaginationParams {
    sort?: string;
}
/**
 * 搜索/排序/分页类型
 *
 * @export
 * @interface FullPaginationParams
 * @extends {PaginationParams}
 */
export interface FullPaginationParams extends PaginationParams {
    search?: string;
    sort?: string;
}
