/**
 * 数字转换
 *
 * @export
 * @param {*} v
 * @returns
 */
export declare function parseNumber(v: any): any;
/**
 * 创建一个a标签触发文件加载
 *
 * @export
 * @param {string} href
 */
export declare function createDownload(href: string): void;
export interface WalkParam {
    [k: string]: string | boolean | number | WalkParam | (WalkParam | number | string | boolean)[] | undefined;
}
/**
 * 解析DTO, 转成字符串
 *
 * @export
 * @param {({ [k: string]: any } | { [k: string]: any} [])} obj
 * @param {string} [prefix='']
 * @returns {string}
 */
export declare function walk(obj: WalkParam | WalkParam[], prefix?: string): string;
