import { StringDTO, BaseTime, BooleanDTO } from '../../define/base';
import { NoteBase } from '../../define/note';
import { TypeWrapper } from '../../http';
import { UserShortInfo } from '../user/types';
import { extend } from '../../define/helper';
export const magazineNote = extend({
    author: UserShortInfo,
    source: StringDTO,
    sticked: BooleanDTO,
}, NoteBase, BaseTime);
export const magazineNoteHttp = TypeWrapper(magazineNote);
