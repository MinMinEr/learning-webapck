import { NumberDTO, StringDTO, BaseTime, BooleanDTO } from '../../define/base';
import { TypeWrapper } from '../../http';
import { extend } from '../../define/helper';
export const BadgeMission = {
    currentNum: NumberDTO,
    targetNum: NumberDTO,
};
export const BadgeBase = extend({
    id: NumberDTO,
    type: StringDTO,
    level: StringDTO,
    avatar: StringDTO,
    title: StringDTO,
    description: StringDTO,
    obtainCount: NumberDTO,
}, BaseTime);
export const BadgeBaseHttp = TypeWrapper(BadgeBase);
export const Badge = extend({
    // entryId: NumberDTO,
    // entryType: StringDTO,
    userObtained: BooleanDTO,
    userObtainedCount: NumberDTO,
    userObtainedAt: StringDTO,
    condition: BadgeMission,
    badgeMissionId: NumberDTO,
}, BadgeBase);
export const BadgeHttp = TypeWrapper(Badge);
