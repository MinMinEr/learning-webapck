import { UserShortInfo } from '../user/types';
import { NumberDTO, StringDTO } from '../../define/base';
import { Answer } from '../answer/types';
import { TypeWrapper } from '../../http';
import { omit, pick } from '../../define/helper';
export const WhiteUser = {
    id: NumberDTO,
    skills: StringDTO,
    tags: StringDTO,
    description: StringDTO,
    user: omit(UserShortInfo, ['anonymous', 'bg']),
    answer: pick(Answer, ['id', 'updatedAt', 'contentPlain', 'problemId']),
};
export const WhiteUserHttp = TypeWrapper(WhiteUser);
