import { NumberDTO, StringDTO, BaseTime, Content } from '../../define/base';
import { UserShortInfo } from '../user/types';
import { TypeWrapper } from '../../http';
import { extend } from '../../define/helper';
export const AnswerNote = extend({
    id: NumberDTO,
    rootableId: NumberDTO,
    linkableId: NumberDTO,
    source: StringDTO,
    author: UserShortInfo,
}, BaseTime, Content);
export const AnswerNoteHttp = TypeWrapper(AnswerNote);
