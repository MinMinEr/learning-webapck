import { NumberDTO } from '../../define/base';
import { UserInfo } from '../user/types';
import { TypeWrapper } from '../../http';
import { extend, pick } from '../../define/helper';
export const RankUser = extend({
    userId: NumberDTO,
    total: NumberDTO,
    rank: NumberDTO,
    score: NumberDTO,
    scoreLevel: NumberDTO,
}, pick(UserInfo, ['username', 'avatar', 'name', 'bg', 'org']));
export const RankUserHttp = TypeWrapper(RankUser);
