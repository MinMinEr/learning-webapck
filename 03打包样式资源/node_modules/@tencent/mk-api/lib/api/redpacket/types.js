import { BaseUser, NumberDTO, StringDTO, Rootable, Linkable } from '../../define/base';
import { TypeWrapper } from '../../http';
import { extend } from '../../define/helper';
export const RedpacketBase = extend({
    id: NumberDTO,
    amount: NumberDTO,
    status: StringDTO,
}, BaseUser, Rootable, Linkable);
export const RedpacketBaseHttp = TypeWrapper(RedpacketBase);
