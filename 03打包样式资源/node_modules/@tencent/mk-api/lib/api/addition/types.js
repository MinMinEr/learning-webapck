import { NoteBase } from '../../define/note';
import { TypeWrapper } from '../../http';
import { UserShortInfo } from '../user/types';
import { extend, pick } from '../../define/helper';
export const Addition = extend(pick(NoteBase, ['source', 'rootableId', 'content', 'contentMd', 'createdAt', 'updatedAt', 'id', 'parentId']), {
    author: UserShortInfo,
});
export const AdditionHttp = TypeWrapper(Addition);
