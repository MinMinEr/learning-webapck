import { NumberDTO, Rootable, Linkable, StringDTO, Title } from '../../define/base';
import { TypeWrapper } from '../../http';
import { extend } from '../../define/helper';
import { RedpacketBase } from '../redpacket/types';
export const Message = extend({
    id: NumberDTO,
    type: StringDTO,
    content: StringDTO,
    contentMd: StringDTO,
    operator: StringDTO,
    status: StringDTO,
    createdAt: StringDTO,
    url: StringDTO,
    related: RedpacketBase,
}, Rootable, Linkable, Title);
export const MessageHttp = TypeWrapper(Message);
