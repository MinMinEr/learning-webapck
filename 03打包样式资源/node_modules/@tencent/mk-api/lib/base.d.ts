import { PaginationParams, PlainObj } from './types';
interface HttpBaseConfigType {
    API_BASE: string;
    UPLOAD_BASE: string;
    DEFAULT_PAGE_SIZE: number;
    DEFAULT_PAGE_NO: number;
}
export declare const HttpBaseConfig: HttpBaseConfigType;
/**
 * 设置Http基础配置
 * 包括后端地址/上传地址/默认分页信息
 *
 * @export
 * @param {Partial<HttpBaseConfigType>} config
 */
export declare function setHttpBaseConfig(config: Partial<HttpBaseConfigType>): void;
/**
 * 获取默认的分页信息
 *
 * @export
 * @returns {PaginationParams}
 */
export declare function getDefaultPagination(): PaginationParams;
/**
 * 注入默认参数
 * 目前默认参数只有分页相关
 *
 * @export
 * @template T
 * @param {T} params
 * @returns {(PaginationParams & T)}
 */
export declare function injectDefaultParams<T extends PlainObj>(params?: T): PaginationParams & T;
export {};
