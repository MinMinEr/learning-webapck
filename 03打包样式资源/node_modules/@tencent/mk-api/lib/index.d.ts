import { HttpBaseConfig } from './base';
import { AxiosRequestConfig } from 'axios';
import { AxiosLike } from './types';
export * from './base';
export * from './api';
export * from './http';
export * from './types';
declare type Config = Partial<typeof HttpBaseConfig & AxiosRequestConfig & {
    http: AxiosLike;
    throwResInError: boolean;
}>;
/**
 * 初始化配置
 * 包括
 * HttpBaseConfig: 基础配置
 * axios 配置(如果使用默认axios实例)
 * http 自定义http请求实例, 需要满足AxiosLike要求
 *
 * @export
 * @param {Config} config
 */
export declare function init(config: Config): void;
